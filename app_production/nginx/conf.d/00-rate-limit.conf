# Incluído dentro de `http { }` pela imagem nginx oficial.
# Camada extra contra flood / DDoS na origem (use também Cloudflare "Under Attack" / rate rules).

limit_req_zone $binary_remote_addr zone=perip:10m rate=100r/s;
